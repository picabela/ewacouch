# Pliki wewnetrzne - brak bezposredniego dostepu z przegladarki
# (skladnia dla Apache 2.4 i starszego 2.2)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
